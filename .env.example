RESEND_API_KEY=
RESEND_FROM_EMAIL=
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_STARTER=
STRIPE_PRICE_ONLINE_MONTHLY=
STRIPE_PRICE_INPERSON_SESSION=
STRIPE_PRICE_INPERSON_MONTHLY=
# /start intake flow — one-time $499 quarterly (price_1TeihUCx8rPt5AvKXLVApCIB)
STRIPE_PRICE_QUARTERLY=
NEXT_PUBLIC_CALCOM_LINK=
NEXT_PUBLIC_SITE_URL=

# MailerLite — funnel infrastructure (Phase 0)
MAILERLITE_API_KEY=
MAILERLITE_GROUP_NURTURE_EN=
MAILERLITE_GROUP_NURTURE_ES=

# Supabase — /start waiver intake (server-side only; service role bypasses RLS)
# REQUIRED before /start can run end-to-end. Run the migration in
# supabase/migrations/ then add these. The service role key is SECRET — never
# expose it to the browser (no NEXT_PUBLIC_ prefix).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Optional override; defaults to "waivers"
SUPABASE_WAIVER_BUCKET=
